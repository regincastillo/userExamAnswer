import { styled } from "@mui/material/styles";

export const LoadingWrapper = styled("div")(() => ({
  display: "flex",
  width: '100vw',
  height: '100vh',
  justifyContent: 'center',
  alignItems: 'center'
}));

